{
    "name": "print-engine",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "pnpm -r --filter \"./packages/*\" run build",
        "test": "vitest run",
        "typecheck": "tsc -b && pnpm --filter @print-engine/designer run typecheck"
    },
    "devDependencies": {
        "jsdom": "^29.1.1",
        "typescript": "^7.0.2",
        "vitest": "^4.1.10"
    },
    "packageManager": "pnpm@10.0.0"
}